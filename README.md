# TOCAdemo
A full-stack web application that allows players to see their past and future training sessions. The application should be built as a single page application (SPA) that includes a React-based frontend and a Node.js backend.
